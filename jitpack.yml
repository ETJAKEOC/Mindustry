jdk:
  - openjdk25
before_install:
  - wget https://github.com/sormuras/bach/raw/releases/25/install-jdk.sh
  - source install-jdk.sh --feature 25
  - jshell --version
install:
  - ./gradlew publishToMavenLocal
  - echo -n > gradle.properties #zero out gradle.properties